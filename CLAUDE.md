# CLAUDE.md — AI Assistant Guide for PSMSP/Screening

## Project Overview

**Repository**: PSMSP/Screening
**Status**: Initial setup — no application code has been committed yet.

This file provides context and conventions for AI assistants working in this repository. Update this file as the project evolves.

## Repository State

As of the last update, this repository is empty (no source code, build configuration, or tests). The sections below should be populated once the project scaffolding is in place.

## Directory Structure

<!-- Update this section when the project structure is established -->

```
Screening/
├── CLAUDE.md          # This file — AI assistant guide
└── (empty)            # No source code yet
```

## Development Setup

<!-- Update with actual setup steps once the project is initialized -->

- **Prerequisites**: (to be determined)
- **Install dependencies**: (to be determined)
- **Run the application**: (to be determined)

## Build & Test Commands

<!-- Update with actual commands once build tooling is configured -->

| Task         | Command | Notes |
|--------------|---------|-------|
| Install deps | —       |       |
| Build        | —       |       |
| Test (all)   | —       |       |
| Test (single)| —       |       |
| Lint         | —       |       |
| Format       | —       |       |

## Code Conventions

<!-- Update as patterns emerge in the codebase -->

- Follow the existing code style in any files already present.
- Prefer small, focused commits with clear messages.
- Do not introduce new dependencies without justification.

## Architecture

<!-- Document the system architecture, key modules, and data flow once code exists -->

(To be documented)

## Git Workflow

- **Default branch**: (to be determined — likely `main` or `master`)
- Feature branches should use descriptive names.
- Commit messages should be concise and describe the "why" not just the "what".
- Push changes to the designated feature branch; do not push directly to the default branch.

## Important Notes for AI Assistants

1. **Read before editing** — Always read a file before modifying it.
2. **Minimal changes** — Only change what is necessary to complete the task. Avoid refactoring unrelated code.
3. **No secrets** — Never commit `.env` files, credentials, API keys, or other secrets.
4. **Test your changes** — Run the test suite (once configured) before committing.
5. **Update this file** — When adding significant infrastructure (build tools, test frameworks, CI/CD), update the relevant sections of this CLAUDE.md.
